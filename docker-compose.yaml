version: "3.9"

services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: user
      RABBITMQ_DEFAULT_PASS: pass
    networks:
      - appnet

  db:
    image: postgres:15
    container_name: postgres
    environment:
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: appdb
    ports:
      - "5432:5432"
    networks:
      - appnet

  publisher:
    build: ./publisher
    container_name: publisher
    depends_on:
      - rabbitmq
      - db
    environment:
      - RABBITMQ_URL=amqp://user:pass@rabbitmq:5672/
      - DATABASE_URL=postgres://appuser:secret@db:5432/appdb?sslmode=disable
      - PORT=8080
    expose:
      - "8080"
    ports:
      - "8080:8080"
    networks:
      - appnet

  consumer:
    build: ./consumer
    depends_on:
      - rabbitmq
      - db
    environment:
      - RABBITMQ_URL=amqp://user:pass@rabbitmq:5672/
      - DATABASE_URL=postgres://appuser:secret@db:5432/appdb?sslmode=disable
    deploy:
      replicas: 3
    networks:
      - appnet

  frontend:
    build:
      context: ./frontend
      args:
        REACT_APP_BACKEND_URL: "http://backend:8080"
    container_name: frontend
    depends_on:
      - publisher
    environment:
      - REACT_APP_BACKEND_URL=http://publisher:8080
    ports:
      - "3000:3000"
    networks:
      - appnet

networks:
  appnet:
    driver: bridge
